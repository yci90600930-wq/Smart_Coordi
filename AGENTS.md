# Smart_Coordi Codex Agent Instructions

## 목표
스마트제조 지원사업 코디네이터가 고객사별 정보를 DB화하고, 견적서와 현장 대화 녹음·전사 결과를 사업계획서/수행일지/결과보고서 작성 데이터로 재사용할 수 있는 웹앱을 개발한다.

## 현재 배포 제약
- 당분간 GPT + GitHub만 사용한다.
- 유료 서버/Railway/OpenAI API Key를 필수로 만들지 않는다.
- GitHub Pages 정적 웹앱이 기본 실행 환경이다.
- 백엔드가 없으므로 데이터는 IndexedDB에 저장하고 JSON 백업/복원을 제공한다.
- 실제 고객 데이터/녹음/견적 원본은 GitHub 저장소에 커밋하지 않는다.

## 필수 원칙
1. 입력이 있는 모든 업무 데이터는 등록·조회·수정·삭제(CRUD)가 가능해야 한다.
2. 업체 → 프로젝트 → 견적 → 현장방문 → 전사/근거 → 문제 → 원인 → 개선과제 → H/W/S/W → 데이터 → KPI 구조를 유지한다.
3. 분석 로직이 입력에 없는 사실이나 수치를 만들어서는 안 된다.
4. 정보 상태는 확인됨 / 추정 / 추가확인 필요 / 상충정보로 구분할 수 있어야 한다.
5. 사업계획서·수행일지·결과보고서의 공통 수치는 한 DB 원본을 재사용한다.
6. 금액은 UI에서 천 단위 쉼표로 표시한다.
7. 사업자등록번호·전화번호는 숫자 중심 입력 후 하이픈을 자동 구성한다.
8. 삭제는 사용자 확인 후 수행한다.
9. 녹음/전사는 현장방문 화면 안에서 진행한다. 별도 녹음 탭을 만들지 않는다.
10. API Key 없이 사용할 수 있는 브라우저 로컬 STT를 우선한다. 실시간 보조 전사는 브라우저 지원 기능을 사용할 수 있으나 문서용 최종 전사는 로컬 Whisper를 우선한다.
11. 전사 분석 시 방문목적·주요논의·현장메모뿐 아니라 문제·원인·개선과제·KPI·H/W·S/W까지 동일 프로젝트에 자동 매핑한다. 자동 생성 항목도 반드시 수정·삭제 가능해야 한다.
12. 견적서는 수기 입력과 파일 인식을 모두 지원한다. PDF 텍스트 추출, 스캔 PDF/이미지 OCR, Excel/CSV 파싱 후 품목을 H/W·S/W·기타로 분류하고 사용자가 수정한 뒤 확정 저장한다.
13. 견적서에서 확정한 품목은 H/W·S/W 탭에 연결하고 사업비 총액에 반영한다. 자동 인식값을 최종 확정값으로 취급하지 말고 사용자가 검토·수정할 수 있게 한다.

## 사업비 기본 검증
교육/사례 규칙으로 총사업비 60%, 정부지원금 60%, 자부담 현금 20%, 현물 20%가 사용될 수 있으나 실제 사업 공고가 최우선이다. 프로그램은 비율을 선택/설정 가능하게 유지하고 재원 합계와 총사업비 불일치는 경고한다.

## 개발 우선순위
1. 브라우저 CRUD 안정화
2. 견적서 인식 정확도와 H/W·S/W 자동분류 향상
3. 녹음/전사 안정화와 전사 품질 향상
4. 전사문 → 방문내용 + 문제/원인/개선과제/KPI/H/W/S/W 자동 매핑 정확도 향상
5. 근거 추적(녹취 문장, 견적 원문, 사진) UX
6. 현장 사진 관리
7. 문서 템플릿 데이터 매핑
8. HWPX 생성 방안
9. 추후 중앙 DB 마이그레이션

## 코드 규칙
- 정적 GitHub Pages에서 바로 동작해야 한다.
- 외부 CDN 의존성은 최소화하고 실패 시 원인을 사용자에게 표시한다.
- 브라우저 권한 거부 시 명확한 오류 메시지를 표시한다.
- 서비스워커 캐시 버전을 변경해 수정사항이 즉시 반영되도록 한다.
- 실제 개인/업체 데이터 샘플을 소스에 넣지 않는다.
