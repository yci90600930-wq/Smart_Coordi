# 스마트제조 자동문서 MVP 개발 에이전트

## 담당 범위
이 폴더의 실행 가능한 MVP를 유지·개선한다. PRD, 데이터 모델, 검증 규칙, API, 화면, 공식 HWPX 매핑을 함께 관리한다. 먼저 README.md와 docs/01-PRD.md, docs/02-architecture.md, docs/03-data-lineage.md, docs/04-screen-flow.md를 확인한다. 상위 프로젝트 AGENTS.md의 현장·문서작성 규칙을 따른다.

## 유지할 업무 원칙
- 단일 기업 원장을 기준으로 1차→2차→3차→사업계획서→결과보고서를 생성한다.
- 확인된 사실, 교육 CASE 사실, 사용자 진술, 추정, 분석, 목표, 실적을 구분한다. 근거 없는 숫자를 넣지 않고 지정 보류표현을 사용한다.
- 검토≠선정≠발주≠설치≠성과달성을 유지한다. 방문 당시 상태와 현재 상태를 분리한다.
- 저장에는 버전·변경사유를 남긴다. 이전 문서와 원장 이력을 보존한다.
- 실시간 전사는 확정 텍스트만 저장하고 원문·요약·등록경로를 함께 보존한다. 선택형 OpenAI Realtime API 키는 서버 환경변수에서만 읽고 브라우저·DB·백업에 노출하지 않는다. 발화 숫자와 상태는 원문 근거에 남기되 KPI·예산·구축상태 확정값으로 자동 승격하지 않는다.
- CASE 4의 6→10개/일, 예산·모델명은 docs/03-data-lineage.md에 기록된 출처 범위에서만 사용한다. 측정근거가 없는 진술 실적을 객관적 성과달성으로 바꾸지 않는다.
- sources/는 읽기 전용이다. templates/의 공식 HWPX 원본은 수정하지 않는다. 출력 복사본의 지정 XML 텍스트만 삽입하며 표·서식·이미지·별도 예시를 보존한다.

## 실행·검증
- Windows start.cmd 또는 Python 3.12 이상으로 `python app.py` 실행. 기본 주소 http://127.0.0.1:8765.
- `python -m unittest discover -s tests -v`로 영향받는 도메인·API·HWPX 검증을 수행한다. 테스트는 임시 DB를 사용한다.
- 화면 변경 시 기업 수정→사유와 함께 저장→5종 생성→검증→HWPX 출력 흐름과 필요한 화면 크기를 실제 브라우저에서 확인한다.
- 근거·상태·금액·출력 관련 변경에는 해당 실패를 잡는 의미 있는 테스트를 추가한다.
- README와 설계 문서를 실제 구현에 맞게 유지한다. 테스트 결과와 확인하지 못한 범위를 함께 보고한다.

## 기본 경계
로컬 단일 사용자 MVP이다. 문서작성과 전사문 요약은 규칙 기반이며 AI 프롬프트 패키지를 제공한다. 외부 연결은 사용자가 시작하는 OpenAI Realtime 음성 전사에 한정한다. 공개 배포·실제 사업신청 제출은 별도 요청이 있을 때 처리한다. HWPX 초안은 한컴의 페이지 배치·사진·서명 검토가 완료된 제출본으로 표시하지 않는다.
